name: Deploy to Elastic Beanstalk

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack
        run: corepack enable

      - name: Use PNPM 10
        run: corepack prepare pnpm@10 --activate

      - name: Install dependencies
        run: |
          if [ -f pnpm-lock.yaml ]; then
            pnpm install --frozen-lockfile
          else
            echo "pnpm-lock.yaml not found; installing without frozen lockfile"
            pnpm install
          fi

      - name: Lint
        run: pnpm run lint

      - name: Run unit tests
        run: pnpm run test:unit

      - name: Build application
        run: pnpm run build

      - name: Create deployment package
        run: |
          zip -r app.zip . \
            -x ".git/*" \
               "node_modules/*" \
               "dist/**/*.map" \
               "dist/**/*.d.ts" \
               "tests/*" \
               ".github/*" \
               "*.log" \
               "pnpm-store/*" \
               "coverage/*"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload application bundle
        env:
          EB_S3_BUCKET: ${{ secrets.EB_S3_BUCKET }}
        run: |
          aws s3 cp app.zip "s3://${EB_S3_BUCKET}/movie-be-${{ github.sha }}.zip"

      - name: Deploy to Elastic Beanstalk
        env:
          EB_APP_NAME: ${{ secrets.EB_APP_NAME }}
          EB_ENV_NAME: ${{ secrets.EB_ENV_NAME }}
          EB_S3_BUCKET: ${{ secrets.EB_S3_BUCKET }}
        run: |
          aws elasticbeanstalk create-application-version \
            --application-name "${EB_APP_NAME}" \
            --version-label "${GITHUB_SHA}" \
            --source-bundle S3Bucket="${EB_S3_BUCKET}",S3Key="movie-be-${GITHUB_SHA}.zip"

          aws elasticbeanstalk update-environment \
            --environment-name "${EB_ENV_NAME}" \
            --version-label "${GITHUB_SHA}"

